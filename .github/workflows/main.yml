name: docker-desktop-extension-ci
on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3 # Checks-out your repository
      - name: Install modules
        run: yarn
      - name: Run tests
        run: yarn test

